# Danmaku Studio 0.4.0

- 新增独立 AAP 画面匹配：本地视频 pHash 检索、单调分段关系、无音轨输入、顺序批量处理和取消。候选进入现有检查、项目保存与弹幕导出流程。
- 重复镜头、填充、未观察边界保持可见；当前画面路线需要确认，不自动签发已验证成品。
- 移除 Gemini 云端复核与相关设置，保留本地音频、纯 XML、手动校准及现有素材接入。
- Emby 会话密码绑定服务器与账号，连接变化后重新输入；原生请求限制跨源重定向与响应体大小。
- 在媒体清单、预览和匹配的文件访问前拒绝隐式 UNC/设备路径。
- 更新 XML 解析与依赖安全修复。公开构建会重映射 Rust 构建路径，源码导出使用显式文件集合。

AAP 尚未经过独立实媒体准确率校准；合成视频回归只能证明覆盖场景。安装包不含 FFmpeg、libmpv 或影片。详见 [使用指南](docs/USER_GUIDE.md) 和 [算法限制](docs/ALGORITHMS.md)。
